class stack: 
def__init__(self)
    self._data = []

def push (self, item)
    self._data = []

def is_empty(self)
    return len (self._data) == 0

def pop (self)
    if self is empty():
        raise empty('stack is empty')
    return ln (self._data) == 0